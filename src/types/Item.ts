//Aqui consta todos os types para utilizar na aplicação, os types mais 
//genéricos que poderá ser utilizado em varios locais diferentes

export type Item ={
    id: number;
    nome: string;
    done: boolean;
}